import { ClassValidatorMessageFilter } from './class-validator-message.filter'

describe('ClassValidatorMessageFilter', () => {
  it('should be defined', () => {
    expect(new ClassValidatorMessageFilter()).toBeDefined()
  })
})
